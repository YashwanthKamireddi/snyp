{
  "name": "snyp",
  "version": "1.0.0",
  "description": "Snyp — a full-stack URL shortener (React + Vite frontend, Express + SQLite backend).",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client --include=dev",
    "build": "npm run install:all && npm run build --prefix client",
    "start": "node server/src/index.js",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client"
  },
  "engines": {
    "node": ">=18"
  }
}
